<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
                <img src="{{ asset('images/logo.png') }}" alt="VieVault" height="35" class="me-2">
                VieVault
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarMain">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a href="{{ path('app_home') }}" class="nav-link">Accueil</a>
                    </li>

                    {% if app.user %}
                        {# Menu Administrateur #}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a href="{{ path('app_admin') }}" class="nav-link">Administration</a>
                            </li>

                        {# Menu Notaire — uniquement l'espace notaire #}
                        {% elseif is_granted('ROLE_NOTAIRE') %}
                            <li class="nav-item">
                                <a href="{{ path('app_notaire') }}" class="nav-link">Espace Notaire</a>
                            </li>

                        {# Menu Utilisateur normal #}
                        {% else %}
                            <li class="nav-item">
                                <a href="{{ path('app_dashboard') }}" class="nav-link">Tableau de bord</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('app_vault') }}" class="nav-link">Mon coffre</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('app_beneficiary') }}" class="nav-link">Bénéficiaires</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('app_audit_logs') }}" class="nav-link">Logs</a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('app_profile') }}" class="nav-link">Mon profil</a>
                            </li>
                        {% endif %}

                        <li class="nav-item">
                            <a href="{{ path('app_logout') }}" class="nav-link text-warning">Déconnexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('app_register') }}" class="nav-link">Inscription</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>