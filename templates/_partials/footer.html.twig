<footer class="footer-marine mt-5 pt-5 pb-4 text-white">
    <div class="container">
        <div class="row g-4 mb-4">
            <div class="col-md-4">
                <div class="d-flex align-items-center gap-2 mb-3">
                    <img src="{{ asset('images/logo.png') }}" alt="VieVault" height="30">
                    <span class="fw-bold fs-5">VieVault</span>
                </div>
                <p class="small opacity-75 mb-0">
                    Coffre-fort numérique et gestion d'héritage pour protéger et transmettre vos données sensibles.
                </p>
            </div>
            <div class="col-md-4">
                <h6 class="text-uppercase fw-bold mb-3 small">Navigation</h6>
                <ul class="list-unstyled small">
                    <li class="mb-2"><a href="{{ path('app_home') }}" class="footer-link">Accueil</a></li>
                    <li class="mb-2"><a href="{{ path('app_about') }}" class="footer-link">Qui sommes-nous</a></li>
                    {% if not app.user %}
                        <li class="mb-2"><a href="{{ path('app_login') }}" class="footer-link">Connexion</a></li>
                        <li class="mb-2"><a href="{{ path('app_register') }}" class="footer-link">Inscription</a></li>
                    {% else %}
                        <li class="mb-2"><a href="{{ path('app_dashboard') }}" class="footer-link">Tableau de bord</a></li>
                    {% endif %}
                </ul>
            </div>
            <div class="col-md-4">
                <h6 class="text-uppercase fw-bold mb-3 small">Sécurité</h6>
                <p class="small opacity-75 mb-0">
                    Vos données sont protégées par un chiffrement AES-256-CBC et un système d'audit complet.
                </p>
            </div>
        </div>
        <div class="border-top pt-3 text-center small opacity-75">
            <p class="mb-0">&copy; 2026 VieVault</p>
        </div>
    </div>
</footer>